import { Raleway, Open_Sans } from 'next/font/google';

export const releway = Raleway({subsets: ["latin"], weight: ['400', '700']});
export const openSans = Open_Sans({subsets: ["latin"], weight: ['400', '700']});